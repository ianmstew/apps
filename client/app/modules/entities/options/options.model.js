define(function (require) {
  var Backbone = require('backbone');

  var OptionsModel = Backbone.Model.extend({

    /*
    defaults: {
      id: null,
      name: null
    }
    */
  });

  return OptionsModel;
});
